const Expanded = ({ data,
    columns,
    filterableCols,
    sortableCols,
    tableHeader,
    recordsPerPageOption,
    defaultRecordPerPage,
    uniqueId,
    upDateDat
}) => {
    return (<div>Expanded</div>);
}

export default Expanded;